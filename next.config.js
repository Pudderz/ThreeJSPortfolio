const withSass = require('@zeit/next-sass')

module.exports = {
    // Target must be serverless
    target: "serverless",
      images: {
        domains: ['images.ctfassets.net'],
      },
  };
  
 